import React from "react";
import "./menu.css";
import MetisMenu from "react-metismenu";

const sideMenu = ({ menuContent }) => {
  return <MetisMenu className="menu" content={data} activeLinkFromLocation />;
};

export default sideMenu;
